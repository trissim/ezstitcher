# ezstitcher/io/constants.py
from typing import Set

# Define default image extensions used by microscopy-related storage operations
DEFAULT_IMAGE_EXTENSIONS: Set[str] = {".tif", ".tiff", ".png"} # Add other relevant extensions as needed